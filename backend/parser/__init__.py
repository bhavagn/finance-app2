"""issuer_detect, coordinate (pdfplumber), multiline (UPI merge), statement_meta, signature (layout
fingerprint), llm_fallback, template_learner (LLM-inferred → validate → save), templates/*.

issuer_detect.py, statement_meta.py, and templates/hdfc_savings.py are implemented. signature,
llm_fallback, and template_learner are not implemented yet — see finance-app-spec.md §7 (Phase 2).
"""
