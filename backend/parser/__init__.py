"""issuer_detect, coordinate (pdfplumber), multiline (UPI merge), statement_meta, signature (layout
fingerprint), llm_fallback, template_learner (LLM-inferred → validate → save), templates/*.
Not implemented yet — see finance-app-spec.md §6.
"""
