"""Async job/queue consumer that drives pipeline.py per statement, off the web request process.

Not implemented yet — see finance-app-spec.md §6 and §9 (runs as its own service, not the API process).
"""
